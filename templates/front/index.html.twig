{% extends 'base.html.twig' %}

{% block title %}Hello FrontController!{% endblock %}

{% block body %}

<div class="container">
    <div class="d-flex justify-content-between">
        {% for book in books %}
            <div>
                <a href="{{path('app_book', {'id' : book.id})}}">{{book.title}}</a>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
